import { TranslationKeyDirective } from './translation-key.directive';

describe('TranslationKeyDirective', () => {
  it('should create an instance', () => {
    const directive = new TranslationKeyDirective();
    expect(directive).toBeTruthy();
  });
});
